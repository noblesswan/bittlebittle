<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="FaqMapper">
<!-- 
    <select id="getFaqList" resultType="Faq">
        select * from faq
    </select>

    <select id="getFaq" parameterType="int" resultType="faq">
        select * from faq where faq_no = #{faqNo}
    </select>

    <insert id="addFaq" parameterType="faq">
        insert into faq (user_no, faq_title, create_date, faq_content) values (#{userNo}, #{faqTitle}, #{createDate}, #{faqContent})
    </insert>

    <update id="updateFaq" parameterType="faq">
        update faq set faq_title = #{faqTitle}, faq_content = #{faqContent} where faq_no = #{faqNo}
    </update>

    <update id="deleteFaq" parameterType="int">
        update faq
        set status = 'N'
        where faq_no = #{faqNo}
    </update>

    <select id="isAuthor" parameterType="map" resultType="Integer">
        select count(*) from faq where faq_no = #{faqNo} and user_no = #{userNo}
    </select>
     -->
</mapper>
